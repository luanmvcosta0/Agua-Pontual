function renderListBreak() {
    const listBreakPage = document.createElement('div');
    listBreakPage.textContent = 'ListBreak Page';
    renderPage(listBreakPage);
  }
  
  function renderAddBreakFast() {
    const addBreakFastPage = document.createElement('div');
    addBreakFastPage.textContent = 'AddBreakFast Page';
    renderPage(addBreakFastPage);
  }
  
  function renderEditBreak() {
    const editBreakPage = document.createElement('div');
    editBreakPage.textContent = 'EditBreak Page';
    renderPage(editBreakPage);
  }